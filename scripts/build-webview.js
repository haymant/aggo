/*
  This file was used for esbuild based webview builds previously.
  The project now uses Vite for webview bundling. Continue to use Vite commands
  (pnpm run build:webview or pnpm run dev:webview) rather than this script.
*/
console.log('esbuild build script is deprecated. Use Vite build (pnpm run build:webview).');
