# packages/core

This package contains built-in components used by the Aggo Page Editor. Components export a `meta`, `schema` and `Component` and are compiled into the webview bundle. Use this folder to author core components and default renderers.

Files:
- `src/index.tsx` — registry map of built-in components and exports.
